version: 0.2

phases:
  install:
    runtime-versions:
      dotnet: 4.8
    commands:
      - echo Installing NuGet and restoring packages...
      - nuget restore WL.PrecisionSample.sln

  build:
    commands:
      - echo Creating artifacts folder...
      - mkdir artifacts
      - echo Building and publishing Members.NewOpinionBar.Web...
      - msbuild Members.NewOpinionBar.Web\Members.NewOpinionBar.Web.csproj `
          /p:DeployOnBuild=true `
          /p:WebPublishMethod=FileSystem `
          /p:Configuration=Release `
          /p:SkipInvalidConfigurations=true `
          /p:DeleteExistingFiles=True `
          /p:publishUrl="artifacts" `
          /p:DeployDefaultTarget=WebPublish
      - echo Build completed. Listing artifacts...
      - dir artifacts

  post_build:
    commands:
      - echo Checking for test DLLs...
      - |
        if (Test-Path "**\bin\Release\*.Tests.dll") {
          vstest.console.exe "**\bin\Release\*.Tests.dll" --logger:trx
        } else {
          echo "No test DLLs found. Skipping tests."
        }

artifacts:
  files:
    - artifacts/**
    - appspec.yml
  discard-paths: yes
  base-directory: '.'
  name: NewOpinionBarWeb-artifact
